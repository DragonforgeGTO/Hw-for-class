#include <iostream>
#include <cstdlib>
#include <ctime>

using namespace std;


int main()
{ 
	unsigned seed = time(0);
	srand(seed);

	int loopy = 25;
	
	for (int potato = 1; potato <= loopy; potato++)

	{
	   const int MIN_VALUE = 0;
	   const int MAX_VALUE = 100000;

	   
	   int num;

       num = (rand() % (MAX_VALUE - MIN_VALUE + 1)) + MIN_VALUE;

	   cout << num << endl;

	} 

	return 0;
}
